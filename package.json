{
  "name": "enterprise-video-platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=auth-service\" \"npm run dev --workspace=video-service\" \"npm run dev --workspace=gateway\""
  },
  "dependencies": {
    "@fastify/http-proxy": "^11.4.1",
    "@fastify/jwt": "^10.0.0",
    "bcrypt": "^6.0.0",
    "fastify": "^5.7.4",
    "ioredis": "^5.10.0",
    "mysql2": "^3.18.2",
    "uuid": "^13.0.0"
  }
}
